torch==2.8.0
torchvision==0.26.0
transformers==5.4.0
accelerate==1.13.0
datasets==4.8.4
safetensors==0.4.0
sentencepiece==0.2.1
protobuf==5.29.6
tqdm==4.67.3
peft==0.18.1
bitsandbytes==0.41.0
scipy==1.11.0
numpy==1.24.0

# Quantization
auto-gptq==0.6.0
autoawq==0.2.9

# GGUF conversion
gguf==0.18.0

# Evaluation
evaluate==0.4.6
# human-eval  # Install separately: pip install human-eval

# Flash Attention (requires Ampere+ GPU, CUDA 11.6+)
# Install with: pip install flash-attn --no-build-isolation
# flash-attn>=2.3.0

# llama.cpp Python bindings for GGUF
# llama-cpp-python>=0.2.0

# vLLM for high-throughput serving
# vllm>=0.2.0

# Docker SDK (optional, for programmatic container management)
# docker>=6.0.0
